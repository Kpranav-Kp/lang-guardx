[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "langguardx"
version = "0.3.0"
description = "Adaptive multi-layer security middleware for LangChain SQL agents"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "K Pranav", email = "pranav2005.kp@example.com" }]

dependencies = [
    # Core utilities
    "mmh3>=3.0.0",
    "bitarray>=2.7.0",
    "huggingface-hub>=0.20.0",
    "safetensors>=0.4.0",
    "sqlglot>=23.0.0",
    "pyyaml>=6.0.0",
    "regex>=2024.0.0",
    "pydantic>=2.0.0",

    # Machine learning
    "torch>=2.0.0",
    "transformers>=4.35.0",
    "scikit-learn>=1.5.0",

    # LangChain ecosystem
    "langchain>=0.1.0",
    "langchain-core>=0.1.0",
    "langchain-community>=0.0.1",
    "langchain-experimental>=0.0.1",
    "langchainhub>=0.1.0",

    # Data and database
    "SQLAlchemy>=2.0.0",
    "numpy>=1.24.0",
    "pandas>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.0",
]

[project.scripts]
langguardx = "lang_guardx.cli:main"
langguardx-verify = "lang_guardx.cli:_cmd_verify_entry"

[tool.hatch.build.targets.wheel]
packages = ["lang_guardx"]
exclude = ["models/", "training/", "tests/", "demo/", "scripts/", "data/"]
